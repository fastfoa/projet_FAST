{% extends 'base.html.twig' %}


{% block body %}
	<div class="container-fluid">
		<div class="row bg-bar">
			<h3> Evaluation {{ user.prenom }} {{ user.nom }} ({{nomFormation}})</h3>
		</div>
			<div class="row">
				<div class="col-12">
					<table class="table-normal">
						<thead>
							<tr>
								<th class="txt-bar bg-bar">Compétence</th>
							</tr>
						</thead>
						<tbody>
							{% for item in list %}
								<tr>
									<td>
										<a href="{{ path( 'saisiEvaluation',{'competence':item.id, 'app':user.id,'session':session.id, } ) }}">
    									    {{item.name}}
										</a>
									</td>
								</tr>
							{% endfor %}
						</tbody>
					</table>
				</div>

			</div>
		{% endblock body %}
