{% extends 'lan/base.html.twig' %}

{% block myStylesheets %}
	<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css" integrity="sha384-50oBUHEmvpQ+1lW4y57PTFmhCaXp0ML5d60M1M7uH2+nqUivzIebhndOJK28anvf" crossorigin="anonymous">
{% endblock %}

{% block title %}Liste des apprentis{% endblock %}

{% block body %}
	<div class="container-fluid bg-workSpace">
			<!-- MAIN CARDS-->
				<div class="row">
				</div>
				<div class="row bg-bar">
					<div class="col-12">
						<h2>Liste Apprentis</h2>
					</div>
				</div>				
				<div class="row bg">
					<div class="col-12">
						<a href="{{ path( 'addUser', {'role':1}) }}">
							<button class="btn btn-success my-2">+ Ajouter un apprenti</button>
						</a>
					</div>
				</div>
				<div class="row">
					<div class="col-12">
						<table class="table-normal">
							<thead>
								<tr>
									<th class="txt-bar bg-bar">Prenom</th>
									<th class="txt-bar bg-bar">nom</th>
									<th class="txt-bar bg-bar">Tél</th>
									<th class="txt-bar bg-bar">Mail</th>
									<th class="txt-bar bg-bar">Action</th>
								</tr>
							</thead>
							<tbody>
								{% for item in list %}
									<tr>
										<td>{{item.prenom}}</td>
										<td>{{item.nom}}</td>
										<td>{{item.telephone}}</td>
										<td>{{item.mail}}</td>
										{#<td>{{session.formation}}</td>#}
										<td>								
											<a href="{{ path( 'formApprenti',{'session':item.id} ) }}">
													<i class="fas fa-solid fa-download" style="font-size:20px;color:green;"></i>
											</a>
                                        </td>
                                        {#
											<a href="{{ path( 'deleteSession',{'session':session.id} ) }}">
													<div style="font-size:20px;color:red; display:inline ;">&nbspX</div>
											</a>
										</td>
                                        #}
									</tr>
								{% endfor %}
							</tbody>
						</table>
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
