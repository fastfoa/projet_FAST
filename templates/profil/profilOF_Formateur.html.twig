{% extends 'profil/profil.html.twig' %}

{% block myStylesheets %}{% endblock myStylesheets %}


{% block myJavascripts %}{% endblock myJavascripts%}

{% block OF %}
<div class="row">
    <div class="col-3">    
        <h5> {{user.dateNaissance|date("d/m/y")|default('date naiss JJ/MM/AAAA')}} </h5>
        <h5> {{user.adresse}} </h5>
        <h5> {{user.telephone|default('tél XX XX XX XX XX')}} </h5>
        <h3> {{user.DiplomePlusHaut|default('diplome')}} </h3>
      
    </div>
    <div class="col-2">    
    </div>
</div>
<hr>    
<div class="row">
    <div class="col-6">    
        <h3>Mes documents :</h3>
        <label for="listDoc">Chercher un document :</label>
        <input list="documents" type="text" id="listDoc">
        <datalist id="documents">
            {% for doc in document %}
                <option id="{{doc.d_id}}" value="{{doc.d_titre}}">{{doc.d_fileName}}</option>
            {% endfor %}
        </datalist>

        {# <a href="{{ path( 'download',{'id':doc.d_id} ) }}">
            <i class="fas fa-solid fa-download" style="font-size:20px;color:green;"></i>
        </a> #}
        <button onclick="getIdOfDatalist();">check</button>
<script>
    function getIdOfDatalist()
    {
        var idI = document.getElementById('listDoc');
        var idD = document.getElementById('documents');

        console.log("idI = " + idI.value);
        console.log("idD = " + idD.options.id);
    }
</script>
    </div>
    <div class="col-2">    
    </div>
</div>
{% endblock OF %}
